# Copy to `.env` in this directory (see repo root `.gitignore`).
# Public Restful Booker Platform demo at https://automationintesting.online — not production secrets.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password
